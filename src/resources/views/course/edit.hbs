<div class="mt-4">
  <h3>Sửa khoá học</h3>
  {{! 
    Hiện giờ form trong html chỉ support GET / POST, nếu nhập
    PUT vào thì mặc định nó sẽ chuyển thành GET, do đó ta dùng thư viện method-override của expressjs.com
    Ta thêm params ?_method="PUT" trong action của form để override method="POST" của form
   }}
  <form method="POST" action="/courses/{{course._id}}?_method=PUT">
    <div class="form-group mt-4">
      <label for="name">Tên Khoá học</label>
      <textarea
        type="text"
        class="form-control"
        name="name"
        id="name"
        value="{{course.name}}"
      >{{course.name}}</textarea>
    </div>
    <div class="form-group">
      <label for="description">Mô tả</label>
      <textarea
        type="text"
        class="form-control"
        name="description"
        id="description"
        value="{{course.description}}"
      >{{course.description}}</textarea>
    </div>
    <div class="form-group">
      <label for="videoId">Video Id</label>
      <textarea
        type="text"
        class="form-control"
        name="videoId"
        id="videoId"
        value="{{course.videoId}}"
      >{{course.videoId}}</textarea>
    </div>
    <div class="form-group">
      <label for="level">Trình độ</label>
      <textarea
        type="text"
        class="form-control"
        name="level"
        id="level"
        value="{{course.level}}"
      >{{course.level}}</textarea>
    </div>
    <button type="submit" class="btn btn-primary mt-4">Lưu lại</button>
  </form>
</div>